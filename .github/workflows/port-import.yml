name: Import Port Config

on:
  push:
    branches:
      - main
    paths:
      - 'production-config/**'

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install Port CLI
        run: npm install -g @port-experimental/port-cli

      - name: Create tar.gz from config
        run: |
          cd production-config
          tar -czf ../production-config.tar.gz .

      - name: Check port import flags
        run: port import --help

      - name: Authenticate Port CLI
        run: |
          port auth login \
            --client-id ${{ secrets.PORT_CLIENT_ID }} \
            --client-secret ${{ secrets.PORT_CLIENT_SECRET }} \
            --api-url https://api.us.getport.io/v1

      - name: Import to Port
        run: |
          port import \
            --input production-config.tar.gz \
            --verbose